services:
  gateway:
    environment:
      MCPGATEWAY_REVERSE_PROXY_ENABLED: ${RP_ENABLED:-true}
      MCPGATEWAY_REVERSE_PROXY_DISTRIBUTED_ENABLED: ${RP_DISTRIBUTED_ENABLED:-true}
      MCPGATEWAY_REVERSE_PROXY_HEARTBEAT_TIMEOUT: ${RP_HEARTBEAT_TIMEOUT:-3}
      GUNICORN_WORKERS: "2"
      LOG_LEVEL: INFO
    deploy:
      replicas: 1
