/**
 * Generated by orval v8.18.0 🍺
 * Do not edit manually.
 * ContextForge
 * ContextForge AI Gateway — an AI gateway, registry, and proxy for MCP, A2A, and REST/gRPC APIs. Exposes a unified control plane with centralized governance, discovery, and observability. Optimizes agent and tool calling, and supports plugins.
 * OpenAPI spec version: 1.0.5
 */
import type { PluginBindingMode } from "./pluginBindingMode";
import type { PluginPolicyItemConfig } from "./pluginPolicyItemConfig";
import type { PluginPolicyItemOnError } from "./pluginPolicyItemOnError";

/**
 * A single plugin policy entry within a team's binding payload.
 *
 * Attributes:
 *     tool_names: List of tool names this policy applies to. Use ``["*"]`` for all tools.
 *     plugin_id: The plugin to bind.
 *     mode: Execution mode.
 *     priority: Execution order — lower numbers run first.
 *     config: Plugin-specific configuration.
 */
export type PluginPolicyItem = {
  /**
   * Tool names to apply the policy to; use ['*'] for all tools in the team
   * @minItems 1
   */
  tool_names: string[];
  /** Plugin class name to bind, e.g. 'OutputLengthGuardPlugin' */
  plugin_id: string;
  /** Execution mode: enforce, enforce_ignore_error, permissive, disabled, sequential, concurrent, transform, audit, or fire_and_forget */
  mode?: PluginBindingMode;
  /**
   * Execution priority; lower numbers run first
   * @minimum 1
   * @maximum 1000
   */
  priority?: number;
  /** Plugin-specific configuration. On upsert the entire config is fully replaced; there is no merge with the previously stored config. */
  config: PluginPolicyItemConfig;
  /** Error handling: fail (block on error), ignore (swallow errors), disable (disable plugin on error) */
  on_error?: PluginPolicyItemOnError;
  /** Optional external reference ID for correlating this binding with an upstream system */
  binding_reference_id?: string | null;
};
