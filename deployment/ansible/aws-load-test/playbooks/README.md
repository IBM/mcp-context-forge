# Ansible playbooks for deploying MCP ContextForge to AWS

## Best Practices

- Each playbook should be self-supporting, gathering any needed metadata from
  AWS rather than depending on previous playbooks.
